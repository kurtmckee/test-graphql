on:
  workflow_dispatch: null
  workflow_run:
    workflows:
      - "✨ Updates"
    types:
      - completed
  pull_request: null


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          env
